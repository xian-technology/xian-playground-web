# Tests

## Purpose

Pytest coverage for the playground's session, validation, and visibility
rules.

## Contents

- `unit/` — isolated behavior such as bootstrap loading, contract-name
  validation, and contract visibility.

## Notes

- Tests run against the local `xian-contracting` harness; sibling checkouts
  must be present as described in the root [`README.md`](../README.md).

## Next

- Run `uv run pytest` from the repo root.
